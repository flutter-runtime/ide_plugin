name: ide_plugin
description: flutter_runtime_ide  基础插件,用于处理 IDE 和插件通信
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  args: ^2.3.1
  plugin_channel:
    git:
      url: "https://github.com/flutter-runtime/plugin_channel.git"
      ref: main

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
